import{readFileSync,writeFileSync}from'node:fs';import{PNG}from'pngjs';
const a=readFileSync('validation/text-spans-metrics.ppm','utf8').trim().split(/\s+/).slice(4).map(Number),results={};for(const name of ['reference','chromium']){const b=PNG.sync.read(readFileSync(`validation/text-spans-${name}.png`)).data;let sum=0,sq=0,max=0,bad=0;for(let i=0;i<4096;i++){let peak=0;for(let c=0;c<3;c++){const d=Math.abs(a[i*3+c]-b[i*4+c]);sum+=d;sq+=d*d;max=Math.max(max,d);peak=Math.max(peak,d);}bad+=peak>32;}results[name]={mae:sum/12288,rmse:Math.sqrt(sq/12288),max,pixelsAbove32:bad};}console.log(results);writeFileSync('validation/text-metrics-experiment-report.json',JSON.stringify({experimental:true,source:'text-investigation/svg.bend',fixture:'text-spans.svg',results},null,2)+'\n');
