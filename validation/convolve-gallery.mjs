import{readFileSync,writeFileSync}from'node:fs';import{dirname,resolve}from'node:path';import{PNG}from'pngjs';
const v=dirname(import.meta.filename),report=JSON.parse(readFileSync(resolve(v,'convolve-reference-report.json'),'utf8')),firefox=JSON.parse(readFileSync(resolve(v,'convolve-firefox-report.json'),'utf8'));let cards='';
for(const row of report.results){const name=row.fixture.slice(0,-4),a=readFileSync(resolve(v,name+'.ppm'),'utf8').trim().split(/\s+/).slice(4).map(Number),png=new PNG({width:64,height:64});for(let i=0;i<4096;i++){for(let c=0;c<3;c++)png.data[i*4+c]=a[i*3+c];png.data[i*4+3]=255;}writeFileSync(resolve(v,name+'-actual.png'),PNG.sync.write(png));const ff=firefox.results.find(r=>r.fixture===row.fixture),refs={...row.references,firefox:ff};cards+=`<section><h2>${row.fixture}</h2><div class="images"><figure><img src="${name}-actual.png" alt="Bend matrix"><figcaption>Bend</figcaption></figure>${Object.entries(refs).map(([ref,m])=>`<figure><img src="${name}-${ref}.png" alt="${ref} matrix"><figcaption>${ref}<br>MAE ${m.meanAbsoluteError.toFixed(3)} · RMSE ${m.rootMeanSquareError.toFixed(3)}<br>${m.passed?'PASS':'FAIL'} · ${m.pixelsAbove32} pixels &gt;32</figcaption></figure>`).join('')}</div></section>`;}
writeFileSync(resolve(v,'convolve-gallery.html'),`<!doctype html><html lang="en"><meta charset="utf-8"><meta name="viewport" content="width=device-width,initial-scale=1"><title>SVG convolution comparisons</title><style>body{font:15px/1.5 system-ui;margin:28px auto;max-width:1080px;padding:0 20px;background:#f5f4f0;color:#202b32}h1{font-size:28px}h2{font-size:18px}section{border-top:1px solid #bbb;margin-top:24px;padding-top:10px}.images{display:grid;grid-template-columns:repeat(4,1fr);gap:18px}figure{margin:0}img{width:100%;image-rendering:pixelated;border:1px solid #aaa;box-sizing:border-box}figcaption{font-size:12px}@media(max-width:650px){.images{grid-template-columns:repeat(2,1fr)}}p{max-width:960px}</style><h1>SVG convolution: four independent outputs</h1><p>${report.integrated?'Installed':'Candidate'} Bend implementation. All 4096 pixels of each 64×64 white-backed image are compared, with 8×8 coverage sampling in Bend. Limits remain MAE ≤1, RMSE ≤5 and at most 1% of pixels above 32 channel error. Chromium ${report.referenceVersions.chromium}; resvg ${report.referenceVersions.resvg}; Firefox ${firefox.referenceVersion}.</p><p>The alpha fixture retains a bias disagreement. Explicit kernel spacing is ignored by Chromium and resvg in these tests; Firefox is the selected reference for that fixture. <a href="convolve-investigation.md">Explanation and specification sources</a> · <a href="convolve-reference-report.json">Raw Chromium/resvg results</a> · <a href="convolve-firefox-report.json">Raw Firefox results</a></p>${cards}</html>\n`);
console.log('Created convolution gallery with all four outputs for five fixtures');
